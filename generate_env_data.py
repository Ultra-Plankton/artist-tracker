"""
This helper script has been archived. See `archive/generate_env_data.py` for the original implementation.

To restore the original file back to the repository root:

    git mv archive/generate_env_data.py .

This stub intentionally does nothing to avoid accidental execution from the repo root.
"""

if __name__ == "__main__":
    print("This helper was archived. See archive/generate_env_data.py for the original implementation.")
