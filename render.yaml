services:
  - type: web
    name: music-updater-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 discord_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
